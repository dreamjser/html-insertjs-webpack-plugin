# html-insertjs-webpack-plugin

    const HtmlInsertWebpackPlugin = require('./html-insert-webpack-plugin')
    new HtmlInsertWebpackPlugin({
      paths: ['https://cdn.jsdelivr.net/npm/vue/dist/vue.js']
    })
